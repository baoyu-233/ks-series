# ks-Eco-RealEstate 修改日志

## 2026-07-23

- 声明 `folia-supported=true`，地块/房屋快照按所属 region 分片读取，农业与工业福利只在 global owner 解析世界后投递到对应 region。
- 地产异步规划线程不再直接读取 Bukkit 世界；增加 Folia Extra 元数据合同测试。
- 默认构件与 Folia ks-Eco 共用同一 Extra JAR；尚未执行 Folia 实机产权、农业、工业和 3D 预渲染验收。
- 售楼沙盘的 region-owned 方块读取和异步隐藏面裁剪完成后，可按配置把冻结的 READY DTO 按 world/dimension 聚合为一个跨服 PROPERTY 快照，并附带该来源下全部已准备房屋的产权人、展示价最小单位和来源世界；发布仍受核心 property-trade 世界/维度策略约束。
- 同一来源使用单调 revision，后完成的并发聚合不会被旧单栋结果覆盖；双房合同测试确认一个 bundle 同时包含两套资产和展示数据。工作线程不会重新读取 Bukkit `World`。
- 修复 READY 预渲染信封包含可空错误/产权字段时 `Map.copyOf` 抛出空指针、导致 PROPERTY 快照始终发布失败的问题；快照仍保持防御性冻结，缺失产权统一投影为系统未认领资产。
- 14 项测试通过；Leaves 实机售楼沙盘返回 4 栋建筑/1 地块，跨服 PROPERTY 聚合稳定包含 4 套资产。

## 2026-07-22

- 地产读写迁移到异步 DTO 与可信缓存，保护缓存异常时失败关闭。
- 增加区域 3D 售楼沙盘、逐栋预渲染、异步隐藏体素裁剪和房源侧栏。
- 修复地图世界切换、地块优先命中、城区相机移动与方块六面材质。
- 房产抵押/挂牌冲突及企业成交权限、企业公户结算完成接线。
- 9 项测试通过；该日尚未完成 Folia 适配。
